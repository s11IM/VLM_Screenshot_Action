{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Permissions for the main app and region selector windows",
  "windows": ["main", "region-selector"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:event:default"
  ]
}
